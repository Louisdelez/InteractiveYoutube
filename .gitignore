# Dependencies
node_modules/

# Secrets
.env

# Build output
client/dist/
**/target/

# Runtime data (cached playlists, DB)
server/data/*.json
server/data/*.db*
logs/
.playwright-mcp/

# Auto-managed yt-dlp binary (downloaded at boot by ytdlp-updater).
# Root-only — `desktop/src/bin/` (cargo binary sources) must stay tracked.
/bin/

# Release build artifacts (uploaded to GitHub Releases, not tracked)
AppDir/
release-staging/
desktop/target/debian/
dist-staging/koala-tv-v*-windows-x86_64/
dist-staging/*.zip
dist-staging/*.sha256
dist-staging/*-setup.exe
dist-macos/

# Editors
.vscode/
.idea/
*.swp
.DS_Store

# Claude Code project memory (local context, not shared)
CLAUDE.md
