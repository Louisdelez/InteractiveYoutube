# Dependencies
node_modules/

# Secrets
.env

# Build output
client/dist/
**/target/

# Runtime data (cached playlists, DB)
server/data/*.json
server/data/*.db*
logs/
.playwright-mcp/

# Auto-managed binary (downloaded at boot by ytdlp-updater)
bin/

# Release build artifacts (uploaded to GitHub Releases, not tracked)
AppDir/
release-staging/
desktop/target/debian/

# Editors
.vscode/
.idea/
*.swp
.DS_Store

# Claude Code project memory (local context, not shared)
CLAUDE.md
